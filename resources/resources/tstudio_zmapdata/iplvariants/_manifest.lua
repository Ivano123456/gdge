-- IPL Variant file manifest
-- Add new IPL-variant file names here (without .lua extension).
-- The loader (client/iplvariants_file_loader.lua) will automatically load and
-- merge them into Config.IplVariants, and client/iplvariants_loader.lua applies them.
--
-- See _template.lua for the schema. Files starting with "_" (like _template) are
-- documentation only and should NOT be listed here.

return {
    "casadelacima",
    "ushero_nightclub",
}
